# --- Build outputs ---------------------------------------------------------

# Built wasm artifacts (c2w-webvpn-proxy.wasm, out.wasm, playground.wasm, …).
*.wasm

# JS/TS toolchains (Vite, Rollup, TypeScript, esbuild).
node_modules/
dist/
.vite/
*.tsbuildinfo

# Emscripten cache (bindings/wasm builds), wasm-pack output.
.em_cache/
bindings/wasm/build/

# --- example-specific -----------------------------------------------------

examples/alpine-curl/*.png
examples/dockerfile-playground/playground-image/alpine.tar

# --- Editor / OS junk ------------------------------------------------------

.DS_Store
*.swp
*.swo
