# --- Build outputs ---------------------------------------------------------

# Built wasm artifacts (c2w-webvpn-proxy.wasm, out.wasm, playground.wasm, …).
*.wasm

# JS/TS toolchains (Vite, Rollup, TypeScript, esbuild).
node_modules/
build/
dist/
.vite/
*.tsbuildinfo

# Upstream c2w sources cloned into public/ during build-image.sh.
public/_c2w_src/

# --- example-specific -----------------------------------------------------

examples/alpine-curl/*.png
examples/dockerfile-playground/alpine.tar

# --- Editor / OS junk ------------------------------------------------------

.DS_Store
*.swp
*.swo
